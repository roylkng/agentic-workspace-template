# Browser Tests — UI/Playwright Tests
#
# Tests that exercise the UI via a real browser.
# Requires: pip install pytest-playwright && playwright install
#
# Run: pytest -m browser
# Or:  make test-browser
